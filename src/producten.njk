---
title: Producten
layout: "base.njk"
---

<section id="billboard" class="bg-light py-5">
    <div class="container">
        <div class="row justify-content-center">
            <h1 class="section-title text-center mt-4" data-aos="fade-up">Producten</h1>
            <div class="col-md-6 text-center" data-aos="fade-up" data-aos-delay="300">
                <p>Hier vind je een kleine greep uit ons aanbod dat we ook online beschikbaar stellen. Onze volledige
                    collectie kun je bekijken op onze social media kanalen.</p>
            </div>
        </div>
        <div class="row" data-aos="fade-up">
            {%- for post in collections.products -%}
            <div class="col-md-4">
                <article class="post-item">
                    <div class="post-image">
                        <a href="{{ post.url }}">
                            <img src="{{ post.data.image }}" alt="{{ post.data.title }}" class="post-grid-image img-fluid">
                        </a>
                    </div>
                    <div class="post-content d-flex flex-wrap gap-2 my-3">
                        <h5 class="post-title text-uppercase">
                            <a href="index.html">{{ post.data.title }}</a>
                        </h5>
                        <p>{{ post.data.description }}</p>
                    </div>
                </article>
            </div>
            {%- endfor -%}
        </div>
    </div>
</section>

